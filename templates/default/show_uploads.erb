#!/bin/bash

# Generated by Chef for <%= node['fqdn'] %>
# Local modifications will be overwritten.

find <% Array(@unames).each do |uname| %>/home/<%= uname %> <% end -%>-iname *.gz -or -iname *.zip -or -iname *.csv

exit
